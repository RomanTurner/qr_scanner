import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'qr.scanner.test',
  appName: 'qr_scanner',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
